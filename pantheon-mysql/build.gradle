dependencies {
    api project(':pantheon')
    api 'com.mysql:mysql-connector-j:9.0.0'

    api 'com.google.protobuf:protobuf-java:4.28.0' //todo za sto je ovo?
    //to override the version in mysql connector which has vulnerabilities
}